<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%- include('./partials/head') %>
    <title>Home - Blogging Website</title>
    <style>
      .container {
        display: grid;
        grid-template-columns: repeat(4, 1fr); /* 4 equal columns */
        gap: 20px; /* Space between cards */
        margin-top: 30px;
      }

      .card {
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        overflow: hidden;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        display: flex;
        flex-direction: column;
      }

      .card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
      }

      .card-body {
  display: flex;
  flex-direction: column;
  align-items: center; /* Center children horizontally */
  justify-content: center; /* Center children vertically (optional) */
}



      .card img {
        border-top-left-radius: 10px;
        border-top-right-radius: 10px;
        width: 100%;
        height: 200px; /* Fixed height for consistency */
        object-fit: cover;
      }

      .card-title {
        font-size: 1.25rem;
        font-weight: 600;
        color: #333;
        margin-bottom: 10px;
        text-transform: capitalize;
        text-align: center;
      }

      .card-text {
        font-size: 1rem;
        color: #555;
        line-height: 1.6;
        max-height: 120px; /* Set a maximum height */
        overflow-y: auto; /* Allow vertical scrolling when content overflows */
        text-align: justify;
        padding-right: 10px; /* Add some space for the scrollbar */
      }

      .author-info {
        display: flex;
        align-items: center;
        margin-top: 15px;
      }

      .author-info img {
        border-radius: 50%;
        width: 40px;
        height: 40px;
        object-fit: cover;
        margin-right: 10px;
      }

      .author-info span {
        font-size: 0.875rem;
        color: #555;
      }

      .btn-primary {
        background-color: #00f0f0;
        border: none;
        padding: 10px 20px;
        font-size: 1rem;
        text-transform: uppercase;
        border-radius: 5px;
        cursor: pointer;
        align-self:center; /* Align button to the left */
        margin-top: 10px;
        flex: content;
        justify-content: center;
      }

      .btn-primary:hover {
        background-color: rgb(0, 199, 182);
      }
      .login-buttons a {
        display: inline-block;
        background-color: #007bff;
        color: white;
        padding: 10px 20px;
        margin: 10px;
        border-radius: 5px;
        text-decoration: none;
        font-size: 1rem;
        text-transform: uppercase;
        transition: background-color 0.3s ease, transform 0.3s ease;
      }

      .login-buttons a:hover {
        background-color: #0056b3;
        transform: scale(1.1);
      }
      .heroContainer {
        margin-top: 50px;
        text-align: center;
      }
      /* Responsive adjustments */
      @media (max-width: 1200px) {
        .container {
          grid-template-columns: repeat(3, 1fr);
        }
      }

      @media (max-width: 900px) {
        .container {
          grid-template-columns: repeat(2, 1fr);
        }
      }

      @media (max-width: 600px) {
        .container {
          grid-template-columns: 1fr;
        }
      }
    </style>
  </head>
  <body>
    <%- include('./partials/navbar') %> <% if (user) { %>
    <div class="container">
      <% if (blogs.length > 0) { %> <% blogs.forEach(blog => { %>
      <div class="card">
        <img
          src="<%= blog.coverImageUrl %>"
          class="card-img-top"
          alt="<%= blog.coverImageUrl %>"
        />
        <div class="card-body">
          <h5 class="card-title"><%= blog.title %></h5>
          <p class="card-text"><%= blog.BlogBody %></p>
          <a href="/blog/<%= blog._id %>" class="btn btn-primary">View</a>

          <!-- Author info section -->
          <div class="author-info">
            <img
              src="<%= blog.createdBy.profileImageUrl %>"
              alt="<%= blog.createdBy.role %>"
            />
            <span
              >Created by: <%= blog.createdBy.userName %> </span
            >
          </div>
        </div>
      </div>
      <% }) %> <% } else { %>
      <div
        class="no-blogs"
        style="
          text-align: center;
          background-color: #f9f9f9;
          padding: 20px;
          border-radius: 10px;
        "
      >
        <h2
          style="
            font-size: 1.5rem;
            flex: content;
            justify-content: center;
            text-align: center;
            color: #555;
          "
        >
          No blogs available! 📝
        </h2>
        <p style="font-size: 1rem; color: #777; margin: 10px 0">
          Create your first blog now and share your thoughts with the world!
          🌟✨
        </p>
        <a
          href="/blog/add-new"
          class="btn btn-primary"
          style="display: inline-block; margin-top: 10px"
          >Create Blog</a
        >
      </div>
      <% } %>
    </div>
    <% } else { %>
        <div class="heroContainer">
            <h1>🌟 Welcome to the Blogging Website!</h1>
            <p>📚 Share your thoughts, ideas, and experiences with the world.</p>
            
            <div
              style="
                max-width: 800px;
                margin: 50px auto;
                padding: 20px;
                background: linear-gradient(135deg, #f0f8ff, #e6f7ff);
                border-radius: 20px;
                box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
                text-align: center;
                font-family: 'Arial', sans-serif;
                line-height: 1.6;
              "
            >
              <h1 style="font-size: 2rem; margin-bottom: 15px">
                🌟 Welcome to the Blogging Hub!
              </h1>
              <p style="font-size: 1.2rem; color: #555; margin-bottom: 20px">
                🖋️ <strong>Express Yourself:</strong> Share your thoughts, ideas, and
                stories with the world!<br />
                📸 <strong>Visualize:</strong> Upload beautiful cover images to
                captivate readers.<br />
                🤝 <strong>Connect:</strong> Join a community of like-minded bloggers
                and readers.<br />
                🔐 <strong>Easy Access:</strong>
                <div class="login-buttons">
                  <a href="/login">🔐 Login</a>
                  <a href="/signup">✍️ Signup</a>
                </div> to get started!
              </p>
            </div>
          </div>
    <% } %> <%- include('./partials/script') %>
  </body>
</html>
